<footer id="footer">
  <div class="footer-inner">
    <div class="footer-brand">
      <span class="footer-logo">ishistory</span>
      <p class="footer-tagline">Long-form history for curious people.</p>
    </div>
    <nav class="footer-nav">
      <div class="footer-col">
        <span class="footer-col-label">Series</span>
        <a href="/ai-history/">History of AI</a>
        <a href="/internet-history/">History of the Internet</a>
        <a href="/robotics/">History of Robotics</a>
      </div>
      <div class="footer-col">
        <span class="footer-col-label">Site</span>
        <a href="/search/">Search</a>
        <a href="/feed.xml">RSS Feed</a>
      </div>
    </nav>
  </div>
  <div class="footer-bottom">
    <span>© {new Date().getFullYear()} ishistory</span>
    <span class="footer-sep">·</span>
    <span>Written with care</span>
  </div>
</footer>
<style>
#footer {
  background: var(--deep);
  border-top: 1px solid var(--border-dim);
  padding: 4rem 2rem 2rem;
  position: relative; z-index: 1;
}
.footer-inner {
  max-width: var(--max-w); margin: 0 auto;
  display: flex; gap: 4rem; flex-wrap: wrap;
  padding-bottom: 3rem;
  border-bottom: 1px solid var(--border-dim);
}
.footer-logo {
  font-family: var(--font-serif); font-size: 1.2rem;
  font-style: italic; color: var(--cream);
  display: block; margin-bottom: 0.6rem;
}
.footer-tagline {
  font-family: var(--font-body); font-size: 0.95rem;
  color: var(--dust); font-style: italic;
}
.footer-nav {
  display: flex; gap: 4rem; flex: 1; justify-content: flex-end;
}
.footer-col {
  display: flex; flex-direction: column; gap: 0.6rem;
}
.footer-col-label {
  font-family: var(--font-sans); font-size: 0.58rem;
  letter-spacing: 0.2em; text-transform: uppercase;
  color: var(--amber); margin-bottom: 0.4rem;
}
.footer-col a {
  font-family: var(--font-body); font-size: 0.95rem;
  color: var(--dust); transition: color 0.2s;
}
.footer-col a:hover { color: var(--fog); }
.footer-bottom {
  max-width: var(--max-w); margin: 1.5rem auto 0;
  display: flex; align-items: center; gap: 0.8rem;
  font-family: var(--font-sans); font-size: 0.58rem;
  letter-spacing: 0.12em; text-transform: uppercase;
  color: var(--muted);
}
.footer-sep { color: var(--clay); }
@media (max-width: 640px) {
  .footer-nav { justify-content: flex-start; gap: 2rem; }
}
</style>